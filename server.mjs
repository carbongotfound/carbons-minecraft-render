import http from'node:http';import fs from'node:fs';import fsp from'node:fs/promises';import path from'node:path';import{fileURLToPath}from'node:url';const root=path.join(path.dirname(fileURLToPath(import.meta.url)),'public'),port=Number(process.env.PORT)||10000,types={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json; charset=utf-8','.png':'image/png','.jpg':'image/jpeg','.jpeg':'image/jpeg','.svg':'image/svg+xml','.ico':'image/x-icon'};const send=(res,code,body,type='text/plain; charset=utf-8')=>{res.writeHead(code,{'content-type':type,'x-content-type-options':'nosniff','referrer-policy':'same-origin'});res.end(body)};http.createServer(async(req,res)=>{try{const u=new URL(req.url,'http://local');if(u.pathname==='/healthz')return send(res,200,JSON.stringify({ok:true,service:'carbons-minecraft'}),'application/json; charset=utf-8');let rel=decodeURIComponent(u.pathname).replace(/^\/+/, '')||'index.html';if(rel.includes('..'))return send(res,400,'Bad path');let file=path.join(root,rel),st=await fsp.stat(file).catch(()=>null);if(!st?.isFile()){file=path.join(root,'index.html');st=await fsp.stat(file)}const ext=path.extname(file).toLowerCase();res.writeHead(200,{'content-type':types[ext]||'application/octet-stream','x-content-type-options':'nosniff','referrer-policy':'same-origin','cache-control':['.html','.js','.css'].includes(ext)?'no-cache':'public, max-age=3600','content-length':st.size});fs.createReadStream(file).pipe(res)}catch{send(res,500,'Server error')}}).listen(port,'0.0.0.0',()=>console.log('Carbons Minecraft listening on 0.0.0.0:'+port));